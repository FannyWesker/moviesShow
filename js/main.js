$(function(){
  init();
});

function init(){
  $('.dropdown-toggle').dropdown();
  //发请求获取json，再把这个json写入默认参数中。
}


// function Sortshow(){
  // this.settings = {
    // //默认参数
  // };
// }

// Sortshow.prototype.frist = function(opt){
  // extend(this.settings,opt);
// };

// function extend(obj1,obj2){
  // for(var attr in obj2){
    // obj1[attr] = obj2[attr];
  // }
// }
